#optimal approach
class Solution:
    def leaders(self, nums):
        maxi = float('-inf')
        n = len(nums)
        ans = []
        for i in range(n-1,-1,-1):
            if nums[i]>maxi:
                maxi = nums[i]
                ans.append(nums[i])
        return ans[::-1]

