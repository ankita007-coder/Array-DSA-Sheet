#User function Template for python3
#optimal approach
class Solution:
    def secondLargestElement(self, nums):
        second_largest,largest = float('-inf'),float('-inf')
        for i in range(len(nums)):
            if nums[i]>largest:
                second_largest = largest
                largest = nums[i]
            elif nums[i]>second_largest and nums[i]<largest:
                second_largest=nums[i]
            
        return -1 if second_largest==float('-inf') else second_largest
#better approach
class Solution:

    def print2largest(self,arr, n):
        # code here
        maxi = -1
        second = -1
        for i in range(n):
            if arr[i]>maxi:
                maxi = arr[i]
        for i in range(n):
            if arr[i]>second and maxi!=arr[i]:
                second = arr[i]
        
        return second        
                
            
